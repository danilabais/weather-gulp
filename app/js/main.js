const swiper = new Swiper('.content', {
    // Optional parameters
    //loop: true,
  
    // If we need pagination
   
  
    // Navigation arrows
    
    mousewheel: {
      sensitivity:1,
    },
    speed:350,
    slidesPerView:2.6,
    watchOverflow:true,
    centeredSlides: true,
    grabCursor:true,
    slideToClickedSlide:true,
    spaceBetween: 250,
   
   
   
  });